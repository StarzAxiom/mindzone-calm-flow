import { useNavigate } from "react-router-dom";

export const MiniPlayer = () => {
  const navigate = useNavigate();

  // Mini player hidden for now - click Music tab in bottom nav to access full player
  return null;
};
